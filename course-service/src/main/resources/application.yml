server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/elearning_db
    username: testuser
    password: testpassword
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
